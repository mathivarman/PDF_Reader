# PythonAnywhere Optimized Requirements
# Optimized for free tier (512MB RAM, 1GB storage)

# Core Django
Django==4.2.7
django-crispy-forms==2.0
crispy-bootstrap5==0.7

# Database
mysqlclient==2.1.1

# Environment variables
python-decouple==3.8

# PDF Processing (lightweight)
PyPDF2==3.0.1
pdfplumber==0.9.0

# Text Processing (optimized for memory)
spacy==3.6.1
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.6.0/en_core_web_sm-3.6.0-py3-none-any.whl

# AI/ML (lightweight models)
sentence-transformers==2.2.2
transformers==4.33.2
torch==2.0.1+cpu
torchvision==0.15.2+cpu
torchaudio==2.0.2+cpu
--find-links https://download.pytorch.org/whl/torch_stable.html

# Hugging Face (free tier)
huggingface-hub==0.16.4

# Utilities
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0

# File handling
Pillow==10.0.0

# Security
cryptography==41.0.3

# Performance monitoring (lightweight)
psutil==5.9.5

# Note: These packages are optimized for PythonAnywhere free tier
# - Using CPU-only PyTorch to save memory
# - Using smaller spaCy model (en_core_web_sm)
# - Minimal dependencies to stay within 1GB storage limit
